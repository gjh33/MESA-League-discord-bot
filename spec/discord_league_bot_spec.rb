require "spec_helper"

describe DiscordLeagueBot do
  it "has a version number" do
    expect(DiscordLeagueBot::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
